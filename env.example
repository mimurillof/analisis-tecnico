# =============================================================================
# CONFIGURACIÓN DE SUPABASE
# =============================================================================

# URL de tu proyecto Supabase
# Obtenerla en: Supabase Dashboard > Settings > API > Project URL
SUPABASE_URL=https://tu-proyecto.supabase.co

# Anon Key (clave pública - para frontend si se implementa)
# Obtenerla en: Supabase Dashboard > Settings > API > Project API keys > anon
SUPABASE_ANON_KEY=tu_anon_key_aqui

# Service Role Key (SOLO PARA BACKEND - NUNCA EXPONER EN CLIENTE)
# Obtenerla en: Supabase Dashboard > Settings > API > Project API keys > service_role
# ADVERTENCIA: Esta key tiene privilegios de administrador y omite RLS
SUPABASE_SERVICE_ROLE=tu_service_role_key_aqui

# Nombre del bucket de Storage (default: portfolio-files)
SUPABASE_BUCKET_NAME=portfolio-files

# Prefijo base para organización (opcional)
# Puede ser un ID de organización o proyecto
SUPABASE_BASE_PREFIX=

# Habilitar/deshabilitar subida a Supabase (útil para testing)
# true = subir archivos a Supabase
# false = generar archivos pero NO subirlos (solo local/logs)
ENABLE_SUPABASE_UPLOAD=true

# Limpiar archivos de prueba después de tests
SUPABASE_CLEANUP_AFTER_TESTS=false


# =============================================================================
# CONFIGURACIÓN DEL SISTEMA DE ANÁLISIS
# =============================================================================

# Intervalo entre ejecuciones (en minutos)
# Recomendado: 15-60 minutos para no sobrecargar APIs
SVGA_INTERVAL_MINUTES=15

# Número máximo de workers para procesamiento paralelo
# - Para Heroku Eco (512MB RAM): 1 (secuencial)
# - Para Heroku Basic/Standard: 2-3
# - Para plan superior: 3-5
MAX_WORKERS=1

# Ejecutar solo una vez y salir (útil para testing)
# true = ejecutar una vez
# false = ejecución continua
RUN_ONCE=false


# =============================================================================
# CONFIGURACIÓN OPCIONAL
# =============================================================================

# Puerto para servidor web (si se implementa dashboard futuro)
PORT=8080

# Nivel de logging
LOG_LEVEL=INFO

